#pragma once

#include "../utils/Vector.h"
#include "Texture.h"

#include <unordered_map>

namespace vlk
{/*
	class SpriteFont : public TextureAtlas
	{
		public:
		SpriteFont();
		virtual ~SpriteFont();
		
		Int GetWidth(const std::string& str) const;
		Int GetFontHeight() const;
		const Texture2D* GetChar(UInt encoding) const;

		void AddCharEncoding(UInt encoding, const Texture2D* texture);
		void SetDefaultChar(UInt encoding);

		private:
		std::unordered_map<UInt, const Texture2D*> encodings;
		const Texture2D* defaultChar;
	};*/
}
